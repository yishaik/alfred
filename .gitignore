# Secrets — never commit
.env

# Runtime state / logs
bridge.log
bridge.log.old
restart.log
run.log
aq.jsonl
session_id.txt
state/

# Python
__pycache__/
*.py[cod]
.venv/
collapse.log
.env.*
*.bak-*

# ...but the committed template must be tracked
!.env.example
